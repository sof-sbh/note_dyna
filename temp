
fetch logs, from: toTimestamp("2026-07-23T12:00:00"), to: toTimestamp("2026-07-23T16:00:00")
| filter loglevel == "ERROR"
| summarize count(), by: {dt.entity.service}
| sort `count()` desc

fetch logs, from: toTimestamp("2026-07-23T12:00:00"), to: toTimestamp("2026-07-23T16:00:00")
| filter loglevel == "ERROR"
| summarize count(), by: {host.name}
| sort `count()` desc
